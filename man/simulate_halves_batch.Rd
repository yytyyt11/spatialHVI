% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_halves.R
\name{simulate_halves_batch}
\alias{simulate_halves_batch}
\title{Generate Halves-Ordered Simulation Datasets in Batch}
\usage{
simulate_halves_batch(
  group_cfgs,
  sigma_block,
  base_dir = ".",
  summary_path = file.path(base_dir, "batch_generation_summary.csv"),
  snp_pattern = "^SNP[0-9]+\\\\.csv$",
  seed_offset = 777
)
}
\arguments{
\item{group_cfgs}{A named list of group configurations. Each element must
define \code{c_traits} (or \code{C}), \code{rho}, \code{knn_k}, \code{knn_sigma}, \code{seed_a} (or
\code{seedA}), and \code{sigma2_pairs}.}

\item{sigma_block}{A 2 by 2 residual covariance matrix shared across groups.}

\item{base_dir}{Directory containing the group folders.}

\item{summary_path}{Optional CSV path for the batch summary. Use \code{NULL} to
skip writing the summary to disk.}

\item{snp_pattern}{Regular expression used to find SNP CSV files inside each
group directory.}

\item{seed_offset}{Integer seed offset used when generating replicate-level
randomness.}
}
\value{
A data frame summarizing all generated datasets.
}
\description{
Reads SNP files from one or more group directories, constructs a shared
adjacency matrix for each group, and writes simulated phenotypes and latent
components for every SNP file that matches \code{snp_pattern}.
}
\examples{
\dontrun{
cfg <- list(
  output_SNP_demo = list(
    c_traits = 4,
    rho = 0.5,
    knn_k = 1,
    knn_sigma = 1,
    seed_a = 42,
    sigma2_pairs = c(1, 2)
  )
)
simulate_halves_batch(cfg, sigma_block = diag(c(1, 1)))
}
}
