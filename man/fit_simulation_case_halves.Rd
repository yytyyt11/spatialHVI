% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vi_fit.R
\name{fit_simulation_case_halves}
\alias{fit_simulation_case_halves}
\title{Fit One Simulated Halves Dataset}
\usage{
fit_simulation_case_halves(
  y,
  k_mat,
  a_true,
  rho_grid = seq(0, 0.99, by = 0.01),
  max_iter = 5000,
  tol = 1e-04,
  verbose = TRUE,
  prefix = NULL
)
}
\arguments{
\item{y}{A halves-ordered phenotype matrix.}

\item{k_mat}{A subject-by-subject kinship matrix.}

\item{a_true}{The adjacency matrix used to simulate the data.}

\item{rho_grid}{Candidate rho values.}

\item{max_iter}{Maximum number of VI iterations.}

\item{tol}{Absolute ELBO tolerance used for convergence.}

\item{verbose}{Logical; if \code{TRUE}, prints ELBO progress.}

\item{prefix}{Optional file prefix passed to \code{\link[=save_vi_result]{save_vi_result()}}. Use \code{NULL} to
skip writing files.}
}
\value{
A list with elements \code{res} and \code{h}.
}
\description{
Runs the heteroscedastic VI model for a single simulated dataset and returns
both the fitted variational object and heritability summaries.
}
\examples{
set.seed(1)
x <- matrix(rnorm(48), nrow = 8)
y <- matrix(rnorm(32), nrow = 4)
a_true <- matrix(c(0, 1, 1, 0), nrow = 2)
out <- fit_simulation_case_halves(
  y = y,
  k_mat = build_kinship_matrix(x),
  a_true = a_true,
  rho_grid = seq(0, 0.2, by = 0.1),
  max_iter = 2,
  tol = 0,
  verbose = FALSE
)
out$h$H2
}
