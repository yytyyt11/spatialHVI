% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vi_fit.R
\name{fit_simulation_case_halves}
\alias{fit_simulation_case_halves}
\title{Fit One Simulated Halves Dataset}
\usage{
fit_simulation_case_halves(
  y,
  k_mat,
  a_true,
  rho_grid = seq(0, 0.99, by = 0.01),
  max_iter = 5000,
  tol = 1e-04,
  verbose = TRUE,
  prefix = NULL
)
}
\arguments{
\item{y}{Numeric simulated phenotype matrix \eqn{Y} with dimension
\eqn{C \times n}: traits in rows, subjects in columns, and rows in halves
order \verb{(L1, ..., LJ, R1, ..., RJ)}. This should usually be the
\code{simulated_phenotypes.csv} output generated by \code{\link[=simulate_halves_batch]{simulate_halves_batch()}}.
Its subject order must match \code{k_mat}. If a transposed matrix or pairs-order
matrix is supplied, the VI model and heritability summary will be fit to
the wrong trait structure or fail dimension checks.}

\item{k_mat}{Numeric kinship matrix \eqn{K} with dimension \eqn{n \times n}
in the same subject order as the columns of \code{y}. In simulation workflows it
is usually computed from the SNP file by \code{\link[=build_kinship_matrix]{build_kinship_matrix()}}. The fit
stops if \code{nrow(k_mat) != ncol(y)}.}

\item{a_true}{Numeric adjacency matrix \eqn{A} with dimension
\eqn{J \times J}, where \eqn{J = C/2}. This is the pair-level adjacency used
to generate the simulated residual covariance, typically read from
\code{A_used.csv}. It is passed as the fixed \code{a_mat} for fitting and for
\code{\link[=heritability_from_vi]{heritability_from_vi()}}, so its pair order must match the halves ordering
of \code{y}.}

\item{rho_grid}{Numeric vector of candidate \eqn{\rho} values used for the
discrete variational posterior. The default \code{seq(0, 0.99, by = 0.01)} is a
broad nonnegative search grid. Narrow it around the simulation truth for
faster experiments, or widen/refine it when assessing sensitivity.}

\item{max_iter}{Positive integer maximum number of VI iterations; default
\code{5000}. Increase when the ELBO continues changing at the limit; reduce for
quick simulation smoke tests.}

\item{tol}{Non-negative numeric ELBO convergence tolerance; default \code{1e-4}.
Use \code{0} to disable early stopping before \code{max_iter}. A large tolerance can
stop before the simulated posterior has stabilized.}

\item{verbose}{Logical scalar controlling ELBO messages. Default \code{TRUE};
set \code{FALSE} when fitting many simulated SNP datasets.}

\item{prefix}{Optional character file prefix passed to \code{\link[=save_vi_result]{save_vi_result()}}.
When \code{NULL} (default), no result files are written. When supplied, files
such as \code{"<prefix>_full_result.rds"} are created and existing files with
those names are overwritten by the underlying write calls.}
}
\value{
A list with elements \code{res} and \code{h}.
}
\description{
Runs the heteroscedastic VI model for a single simulated dataset and returns
both the fitted variational object and heritability summaries.
}
\examples{
set.seed(1)
x <- matrix(rnorm(48), nrow = 8)
y <- matrix(rnorm(32), nrow = 4)
a_true <- matrix(c(0, 1, 1, 0), nrow = 2)
out <- fit_simulation_case_halves(
  y = y,
  k_mat = build_kinship_matrix(x),
  a_true = a_true,
  rho_grid = seq(0, 0.2, by = 0.1),
  max_iter = 2,
  tol = 0,
  verbose = FALSE
)
out$h$H2
}
