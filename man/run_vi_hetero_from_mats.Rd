% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vi_fit.R
\name{run_vi_hetero_from_mats}
\alias{run_vi_hetero_from_mats}
\title{Fit the Heteroscedastic Halves VI Model}
\usage{
run_vi_hetero_from_mats(
  y,
  k_mat,
  a_mat = NULL,
  rho_grid = seq(0, 0.99, by = 0.01),
  max_iter = 5000,
  tol = 1e-04,
  verbose = TRUE
)
}
\arguments{
\item{y}{A numeric matrix with traits in rows and subjects in columns, ordered
as \verb{(L1, ..., LJ, R1, ..., RJ)}.}

\item{k_mat}{A subject-by-subject kinship matrix.}

\item{a_mat}{Optional \code{J} by \code{J} adjacency matrix. When \code{NULL}, it is estimated
from \code{y}.}

\item{rho_grid}{Candidate rho values.}

\item{max_iter}{Maximum number of VI iterations.}

\item{tol}{Absolute ELBO tolerance used for convergence.}

\item{verbose}{Logical; if \code{TRUE}, prints ELBO progress.}
}
\value{
A list containing posterior summaries, ELBO history, and fitted
hyperparameters.
}
\description{
Fits the fast exact variational inference model for halves-ordered traits.
}
\examples{
set.seed(1)
x <- matrix(rnorm(48), nrow = 8)
k_mat <- build_kinship_matrix(x)
y <- matrix(rnorm(32), nrow = 4)
fit <- run_vi_hetero_from_mats(
  y = y,
  k_mat = k_mat,
  rho_grid = seq(0, 0.2, by = 0.1),
  max_iter = 2,
  tol = 0,
  verbose = FALSE
)
fit$E_rho
}
